// import { ThunkAction } from "redux-thunk";
// import { RootState } from "../store";
// import { addPartners } from "../slice/partners-slice";
// import { getAllPartners } from "@/app/lib/data";

// export const getPartners = (number: number): ThunkAction<void, RootState, unknown, any> => async (dispatch, getState) => {
//     let p = await getAllPartners(number);
//     dispatch(addPartners(p));
// }